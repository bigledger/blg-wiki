# Open questions for Vincent

Ordered by what it costs to leave them open. One line each — the detail is in `open/` where there
is a file. Answer in any form; I will record it in `answered/` and act.

*Last updated 2026-09-06 (Q-0146…Q-0148 added and **Q-0038 answered in full** by the modules/modules-v2 duplication audit — see planning/reviews/2026-09-06-modules-duplication-audit.md; Q-0136 added and Q-0006 re-scoped by the compliance-claim sweep — see planning/drafts/2026-09-06-compliance-claim-sweep.md; Q-0125, Q-0126 added by the sales-workflow lane sweep pass 3; Q-0111…Q-0115 added by the guides + user-guide 110-page audit — see planning/drafts/2026-09-06-guides-audit.md; Q-0093, Q-0094 added by the sales-workflow lane sweep; Q-0083 added by the mermaid-conversion pilot; Q-0060 added by the external-sales-invoice unit; Q-0014 answered by the data-hub integration unit; see planning/drafts/2026-09-06-datahub-integration-path.md). Answered questions move to `answered/` and leave this list.*

## High — work is stopped

| id | question | blocks |
|---|---|---|
| Q-0001 | May we publish the **MyInvois intermediary permission set** a customer must grant BigLedger? Today the setup guide says "grant the full set, ask before you save the form". | The single biggest gap in the e-invoice setup guide (F-0219, F-0238) |
| Q-0002 | Is the **from-IRB inbound consumer** planned, abandoned, or forgotten? blg-intranet#5617 proposes exactly this work. | Whether the new incoming-supplier guide is correct or needs rewriting (F-0274) |
| Q-0003 | **Self-billed e-invoices**: which document types, where is the flag set, can they be consolidated, which LHDN scenarios do our customers use? | A written guide is blocked on it (F-0227) |
| Q-0004 | **Peppol participant-ID scheme** — our guide and our applet page disagree. Which is right? | The Peppol guide rewrite; the current page is the largest live defect in that section (F-0228) |
| Q-0005 | **By-document or by-branch consolidation** — which do we recommend? And by-branch is documented as producing separate foreign-counterparty e-invoices, which contradicts the rule that a consolidated e-invoice cannot carry a foreign buyer. | The month-end and pools guides both hedge (F-0226) |
| Q-0006 | Do we hold **SOC 2 Type II, ISO 27001 or GDPR** certification? The architecture page claimed all three; it is unpublished pending your answer. **Blast radius measured 2026-09-06: 27 more files across four languages carried the same class** — certification bullet lists, "99.9% uptime SLA", "bank-level encryption (AES-256)", "256-bit SSL", "SOC 2 Type II certified infrastructure", "ISO 27001 certified data centers", "PDPA Compliant", "LHDN Approved Solution". All corrected in place (nothing unpublished) and a lint added, so the answer is no longer about one page: **yes** re-opens 27 pages with sourced wording; **no** keeps them as they are. Measurement note: `akaun.com` negotiates TLS 1.3 with **AES-128**-GCM, so "256-bit" was wrong on the facts, not merely unevidenced. See planning/drafts/2026-09-06-compliance-claim-sweep.md. | Whether the claim returns sourced, or never — and whether sales material repeats it (F-0294, F-0462…F-0467, P-0121) |
| Q-0007 | Is there an **MCP server**? `hugo.yaml` claims MCP in four languages; the only mention in any repo is a spec headed *"Why NOT an MCP Server?"*. | The canonical product description in CLAUDE.md (F-0287) |
| Q-0037 | **Is the data in the six membership API pages real captured tenant data?** They carry `tenantCode: testing`, real-looking GUIDs, and four `card_no` values of which three are in Malaysian NRIC format (`930425035604`, `930425035605`, `930425035569`). May they stay, or must they be regenerated? A signed ADMIN JWT was on the same pages and is now scrubbed (P-0062). | Whether the only verified-real API pages outside `developers/` can stay up as they are (F-0325) |
| Q-0070 | **58 of 134 translated pages are publishing content the English no longer says — may they come down?** The Web Hook Chinese twin was one instance of a class. 24 zh and 10 ms applet pages sit under English pages rebuilt from source on 2026-09-05/06; 16 zh pages translate English pages that were deleted; 3 (zh/ms/ar) carry the invented approval matrix removed today; 3 zh pages carry tier-1 invented claims (1,000,000 users and a 99.9% SLA on the Chinese home page, SOC 2 / ISO 27001 on two applet pages, a named customer testimonial with 31 invented statistics); 2 zh e-invoice guides were rewritten underneath. Nothing was unpublished — 58 is far past the ADR-0008 guard rail. Full list with file:line in `planning/reviews/2026-09-06-translation-orphan-sweep.md`. | The Chinese site drops 103 → 45 pages and the Malay 24 → 14 if approved; if not, all 58 stay live (F-0365) |
| Q-0111 | **`guides/advanced/` — seven pages, 8,142 lines, and roughly a dozen sentences that are about BigLedger. May they come down?** Written Aug 2025 as generic ERP-consultant prose with the product name substituted in. `performance-optimization.md` (1,487 lines) tells a SaaS tenant to run `ALTER SYSTEM`, `DROP TABLE`, `COPY … FROM PROGRAM` and `CREATE EXTENSION aws_s3` against a fabricated schema, and prescribes MySQL and SQL Server settings for a PostgreSQL product. `integration-best-practices.md` (1,451 lines) asserts named API integrations with four real Malaysian banks and seven payment gateways, a PCI DSS conformance narrative, GDPR compliance, and a 260-line EDI guide for a product with no EDI applet. `advanced-inventory-manufacturing.md` and `industry-specific-workflows.md` document MRP, MPS, CRP, routings, work centres, ECOs, project accounting, timesheets and progress billing — none of which exist in the registry. `financial-reporting-excellence.md` mentions BigLedger once in 1,278 lines. `compliance-audit.md` and `accounting-workflows.md` carry materially wrong Malaysian tax facts (F-0437). Nothing was unpublished — seven is past the ADR-0008 guard rail. | 7 pages, ~8,100 lines; two of them (performance, integration) I would take down today if the cap allowed (F-0427, F-0428) |
| Q-0112 | **`guides/roles/` — nine pages of unanswered requirements-gathering questions published as user guides. Rewrite, or take down?** All eight role pages are first-person monologues from a prospective buyer — `cashier.md:9` opens *"I'm a cashier at a retail store in Malaysia … I need practical, step-by-step guidance"*; `internal-audit.md:326` opens a section *"When evaluating BigLedger's audit and control capabilities, I will focus on:"*. Between them: 500+ questions, **zero answers, zero numbered steps, zero menu paths, zero screenshots**. They also make capability claims by presupposition (approval limits, delegation controls, AI-powered forecasting, role-specific mobile apps). Separately, `roles/_index.md:101-106` invents a named-role/permission-verb model: `TntErpPermissions.java` is a flat enum of ~7,836 `API_TNT_DM_ERP_<RESOURCE>_<VERB>` constants with **no named-role construct at all**, and only 2 APPROVE verbs, both on the generic-doc open queue. They are linked from eight cards on `guides/_index.md:41-50` under a promise of "8 guides tailored to specific job functions". | 9 pages; the role-based navigation on the guides landing page |
| Q-0113 | **`user-guide/demo/` — 13 pages that publish shared credentials for a host that does not resolve, plus a real person's contact details. Unpublish as a block?** `demo-v1.bigledger.com` is NXDOMAIN (Q-0039) and Q-0024 confirms there is no sandbox; ten of the thirteen pages carry a login block, `Demo2025!` appears 20 times across thirteen usernames, and 222 "Expected Result" assertions describe a system nobody can open. Also on these pages: a named individual's **Telegram handle** (`demo/_index.md:64`, `getting-started-demo:443`) and **work e-mail** (`demo/_index.md:343`, `manufacturing:1131`); two **different company phone numbers** that appear nowhere else on the site and contradict each other (`role-based/_index.md:192` vs `food-beverage:1473`); fabricated **customer testimonials** (`apparel:635-642`, `distributors:623-627`); published **commercial guarantees** ("ROI Guarantee", "90-day go-live guarantee", "Fashion Guarantee") and discount codes on six pages; real vendors used as characters with invented revenue and rebate figures (`distributors:270-291`); and **surviving UTM tender residue** — `demo/_index.md:86, 114-121` still renders an Education tab with "100% federal requirement adherence", and `apparel:655` is a button labelled "Explore University Demo". Same failure class as the pages unpublished this morning. Note the Telegram handle also appears at `user-guide/troubleshooting/_index.md:369`. | 13 pages, ~9,900 lines; and the personal contact details are live now |
| Q-0114 | **`user-guide/` vs `user-guide-v2/` — 15 live pages are unreachable from navigation and one page ships a raw JS redirect. Which set survives?** `user-guide/_index.md:10` carries an inline `<script>` redirecting `/user-guide/` to `/user-guide-v2/`; the v2 hub links only to the three `-v2` sections; and `administration/`, `basic-operations/` and `getting-started/` are `bookHidden` with a cascade — so 15 pages stay live, indexed and the top search result for their own titles while being unreachable from the menu. Seven of those 15 are substantially invented (F-0429, F-0430, F-0432). The three `-v2` indexes are not duplicates — they are YouTube lesson lists — but each carries ~110 lines of inline `<style>`/`<script>`/`<div>`, against the Hextra rule in CLAUDE.md. Do we finish the v2 migration (rebuild or retire the 15), or reverse it? | 15 orphaned live pages plus the redirect (F-0435) |
| Q-0115 | **`guides/einvoice-guides/peppol-configuration.md` is worse than the Q-0004 scheme disagreement and was missed by the e-invoice rewrite wave.** Beyond the participant-ID prefix (`0195:` in the guide vs `0230:` on the applet page — in the Peppol ICD list 0195 is Singapore UEN and 0230 is Malaysia), the guide's menu path *E-Invoice → PEPPOL Settings* names no screen (the applet menu is *Peppol Config → Registration*), it offers Order and Order Response document types for a pipeline that builds only invoices and credit notes, and it adds unsourced digital-signature / timestamp / 7-year-archival compliance claims and a "PEPPOL test network" procedure. `kb/topics/e-invoice-peppol.md:80` already records that it "needs rewriting from scratch". It also publishes `peppol@bigledger.com`, which appears nowhere else in the repo. | The Peppol guide; answering Q-0004 unblocks the rewrite |

## Medium — work continues, degraded

| id | question | blocks |
|---|---|---|
| Q-0008 | Should we **publish `blg-shared-utilities` to npm and give applet registration a self-service path**? Without both, an outside developer can build a bundle but cannot run it. | Whether the Android standard is reachable at all (F-0296) |
| Q-0009 | **ADR-0007**: accept, amend or reject — archive pages for built-but-unregistered applets, and ask the platform team once whether Group Maintenance, E-Mandate, Team Maintenance and Investment are meant to be registered? | Four applets, seven-plus pages (F-0272, F-0282) |
| Q-0010 | **ADR-0006 revisit**: should concept pages be written *after* a module's procedures are complete rather than before? The pilot's own test failed. | Whether the concepts layer rolls out beyond e-invoice (F-0261, F-0276) |
| Q-0011 | Is **four-field exact-equality matching** for incoming e-invoices intended to stay? Nothing has ever matched under it across 89 tenants. | Whether the guide teaches the current rule as permanent (F-0274) |
| Q-0012 | Should the **RM 10,000 individual-submission rule** also block credit notes, debit notes, refund notes and sales returns? Only sales invoices and cash bills are enforced today. | If yes, documents are already at LHDN in a shape LHDN forbids (F-0264) |
| Q-0013 | Is **`developers@bigledger.com`** monitored? The new applet pages use it as the hand-off contact throughout. | Every hand-off in the applet path (F-0297) |
| Q-0015 | **`my-einvoice-for-customer-and-supplier-applet.md`** is substantially invented and walks AP staff to a screen that is empty on every tenant. Rewrite, stub, or unpublish? | (F-0275) |
| Q-0021 | **Is `https://api-etl.akaun.com` the host external integrators should be told to use?** It exists, resolves, and serves the same `/core2/` surface as `api.akaun.com`; every modern BigLedger ETL agent points at it. The new integration pages say to use it for data traffic and `api.akaun.com` for sign-in and key management. Confirm, or tell me the intended split. | The integration path's worked example, and every curl on it (F-0298) |
| Q-0022 | **Is "Operational Data Hub" the public name for the `etl-ep` surface?** The marketing site and nine wiki pages use "data hub"; the product, the registry and the API use no such term, and the engineering copy in the platform says *"Operational Datalake"*. The new pages deliberately name it by what it is rather than invent a brand. If there is an intended public name, say it and I will use it everywhere. | Naming consistency between wiki, product and marketing (F-0298) |
| Q-0023 | **May we publish a worked request body for one business document type?** Reading is fully documented; writing stops at the document schema, because a sales invoice runs through several hundred lines of fill-and-validate and we will not guess it. One verified example — a minimal `INTERNAL_SALES_INVOICE` container that posts cleanly in a known tenant — would close the largest remaining gap in the integration path. Who can produce it? | The write half of the integration path (F-0298) |
| Q-0024 | **Is there a test tenant we can point external integrators at?** There is no sandbox environment and no self-service sign-up that yields a tenant. The pages currently tell integrators to ask for one. Is that ask answerable, and by whom? | Every external integrator's first step (F-0298) |
| Q-0050 | **Which Unified Contact Center should the wiki document?** Two rows are ACTIVE: `UnifiedContactCenter` (2021, route `applets/tnt/ailedger/ucc`, its `documentation_url` points at our page) and a second, newer one marked *internal-tool* in `applet-exclusions.tsv` (route `…/uccjava`). The documented one's repository has not moved since 2026-04-01, and its last commit is the cut-over to the newer version; all development since then is in the excluded repo (HEAD 2026-09-03, ~200 open issues including a full UI revamp epic). Are customers still on the old bundle, is the newer one really internal-only, and when the switch happens does the page follow it? | The UCC page is now written from the 2026-04 source; if tenants are on the newer build the screens and settings may already differ (F-0340) |
| Q-0060 | **Is `external-sales-invoice-applet` meant to be registered, and what is it actually for?** The repo is maintained (HEAD 2026-08-21) and carries a production publish script, but there is no `bl_applet_hdr` row under any name, code, guid or bundle URL — and the script deploys to `s3://…/wavelet-erp/**supplier-sales-invoice-applet**/prod` while the dev `appletCode` is `external-supplier-sales-invoice-applet`. The code is a half-converted Stock Conversion clone that lists external generic documents (`bl_fi_xtn_generic_doc_hdr`) read-only; create, edit and FINAL are commented out. Is this a supplier-submits-their-invoice screen that stalled, or abandoned? That makes the built-but-unregistered list **seven** (see Q-0026, Q-0009). | Whether the applet page ever returns; the page is unpublished today (F-0352) |
| Q-0093 | **Four more built-but-unregistered applets, and this time they are the ones that do the work.** `internal-sales-gin-stock-out-applet`, `internal-sales-invoice-no-stock-out-applet`, `internal-delivery-order-processing-applet` and `internal-outbound-delivery-order-applet` all have maintained repos (HEADs 2026-07-15 to 2026-09-01) and no `bl_applet_hdr` row under any code, name, bundle URL or dev `appletGuid`. The pattern matters: the **registered** Sales GIN and Sales GRN applets are quantity-signum 0 / amount-signum 0 and post no journal, while the backend's stock-moving sales types — `INTERNAL_SALES_GIN_STOCK_OUT` (qty −1), `INTERNAL_SALES_GRN_STOCK_IN` (qty +1) and `INTERNAL_SALES_INVOICE_NO_STOCK_OUT` (amt +1) — are exactly the ones whose applets are missing from the registry. That looks like a registration gap, not a design. This makes the built-but-unregistered list **eleven** (Q-0009, Q-0026, Q-0060). | Four pages left live pending your answer, one with 22 inbound links (F-0407) |
| Q-0094 | **Is `InternalDeliveryApplet` (Delivery Order Applet (Internal)) customer-specific?** It is ACTIVE, but its `es_module_url` is the only sales-side bundle served from a **customer-namespaced** S3 prefix rather than `bigledger/wavelet-erp/…`, and no repository in `refs/` builds it. If it is customer-specific it belongs in `applet-exclusions.tsv` and its page comes down; if not, we need to know where the source lives. Its wiki page is a 12-line stub either way. | Whether `/applets/sales-workflow/internal-delivery-order-applet/` stays (F-0407) |
| Q-0016 | Merges awaiting your word: `mm-deposit-applet` → `deposit-applet`; `general-ledger-applet` (19 broken inbound links); the tax-page merge (F-0210); `customer-applet` → `customer-maintenance` (F-0050). | Several pages and their aliases |
| Q-0025 | **Where does the T2T Admin front end live?** `t2tAdminApplet` is ACTIVE and ships a bundle from the applet CDN, but no repository in the GitHub organisation matches `t2t` (455 enumerated) and nothing in `refs/` builds it. Is the source lost, private, or under a name we would not guess? | The T2T page can document the API but not a single screen or setting (F-0307) |
| Q-0026 | **The built-but-unregistered list is now six**, not four: Transaction Reconciliation and Webhook join Group Maintenance, E-Mandate, Team Maintenance and Investment. Does that change the ADR-0007 answer in Q-0009 — and does the Webhook page's unpublish under ADR-0008 settle it for that one? | Six applets, nine-plus pages (F-0308) |
| Q-0038 | **`modules/` or `modules-v2/` — which one survives?** **Now measured in full (2026-09-06, `planning/reviews/2026-09-06-modules-duplication-audit.md`) and the merge premise is wrong — there is almost nothing to merge.** 51 pages share a path; **21 are byte-identical** (all `membership/`, not six) and 3 near-identical. Of the 27 that diverge, `modules-v2/` is the more correct in every real-module pair, checked against source. The accuracy gap: **`modules-v2/` had 11 broken links (now 0); `modules/` has 206**, and **70 of its distinct `/applets/…` targets name applets with no row in `bl_applet_hdr`** — `erp/_index.md` invents ~34 and `accounting/_index.md` ~19. Five `modules/` pages (`erp`, `it-ce`, `service-industry`, `drop-shipping`, `projects`) have **zero** real applets behind them, and `modules-v2/` already omits all five — the pruning was done, and it was right. The only thing `modules/` uniquely holds is 12 membership configuration pages, which duplicate the sourced `membership-admin-applet` page anyway. **Recommendation: `modules-v2/` survives; `modules/` is unpublished under ADR-0008, not merged** — 9 pages tier-1, 2 applet manuals moved, 4 URL collisions deleted, the membership/claims/ecommerce material migrated forward, `/modules/` aliased to `/modules-v2/`, then `modules-v2/` renamed to `modules/`. That is 15+ pages down, past the five-page stop threshold, so nothing was touched. **The one-word question: `modules/` down — yes or no?** | An ADR + spec; 88 inbound links; 6 broken main-nav entries (Q-0146) |
| Q-0039 | **Six `*.bigledger.com` subdomains do not resolve** — `forum`, `academy`, `demo-v1`, `support`, `community`, `status` — and 56 published pages link to them. Are they planned, dead, or served from somewhere else? | Whether those are 56 links to repair or 56 links to delete (F-0327) |
| Q-0071 | **What is the retranslation path?** We do not author translations (CLAUDE.md), so anything unpublished under Q-0070 stays down until someone else produces one. Is there a vendor, a process, or an accepted answer that these pages simply stay down and the language switcher falls back to English? | Whether Q-0070 is a temporary or a permanent removal (F-0365) |
| Q-0072 | **Can we evidence "1,000,000+ users" and a "99.9% uptime SLA guarantee"?** Both are on the Chinese home page (`content/zh/_index.md:3,17,145,146`) and nowhere in the English. Same family as Q-0006 (SOC 2 / ISO 27001 / GDPR), which is still open. | Whether the numbers are corrected or removed, and whether sales material repeats them (F-0366) |
| Q-0073 | **Should the `zh`/`ms`/`ar` trees mirror the English directory structure, or do we pair by `translationKey` where paths diverge?** 69 translated pages are not path-paired, mostly from the 2026-02/2026-04 applet regrouping. This unit repaired 16 faithful pages with `translationKey`, which restores the language switcher and changes no URL. Mirroring instead means moving 16+ `/zh/` URLs, adding eight new Chinese section `_index.md` files (which is authoring translated content), and adding aliases. Needs an ADR either way; CLAUDE.md currently states the path rule. | How every future English move is handled, and whether the 16 repairs stand (F-0365) |
| Q-0083 | **The generated infographics are wrong now — should they come down now, rather than stay live until redrawn?** F-0380 chose "keep live, redraw later". The pilot that decision asked for converted five of the 75 and found that **five of five assert something source contradicts** — a stock-adjustment approval workflow that does not exist, "Available = Physical − Reserved" when the backend default adds a `+1` goods-received term, "enforce FIFO" from a read-only applet, Chart of Account "role-based access" from an applet that registers no permissions at all, driver status tracking that nothing reads, and an invented product logo ("DeliveryConnect Applet", F-0392). Only two of the five contained a diagram worth keeping; one deserved no replacement at all; roughly 60–70% of each canvas is marketing with no vector equivalent. Removing a reference costs one line and loses nothing, because in every case the page prose was already more correct than the picture. Recommendation: **quarantine the remaining ~52 now, then redraw the 15–20 that turn out to have a real flow or structure** — rather than leave false claims published for the length of the redraw queue. Evidence and per-diagram tables in `planning/drafts/2026-09-06-mermaid-conversion-pilot.md`. | Whether ~52 published pages carry an unverified diagram for the next few weeks, and how the rest of the F-0380 programme is scheduled (F-0391, F-0392, F-0393) |

## Low — tidy-up

| id | question | blocks |
|---|---|---|
| Q-0017 | Delete the two MS ESD pages and the three Team Maintenance stubs? All are customer-specific or empty; one page's entire body is the character `1`. | (F-0270) |
| Q-0018 | Does the small **top-bar avatar** disqualify a screenshot? 232 images are quarantined; 16 kept images show only that. | The recapture backlog (F-0271) |
| Q-0019 | What should happen long-term to the **archived UTM material** — rewrite generically for public sector, or leave archived? | (F-0277, now archived) |
| Q-0020 | Should **`bl_applet_hdr.name` be normalised**? `mySST` is stored upper-case, so "title = registry name exactly" yields a shouting page title. | A wiki exception, or a data fix (F-0281) |

## Added 2026-09-06 (later)

| id | question | blocks |
|---|---|---|
| Q-0100 | **Does the top-bar avatar disqualify a screenshot?** My recommendation: **no** — measured, it is roughly 40×40 px in a 1900 px-wide capture; a person is visible but not identifiable, which is a different thing from the cases we did quarantine (full names in grids, WhatsApp transcripts, mobile numbers, a full-size profile photo as the page's subject). **943 screenshots are currently published** and nearly all carry it; treating it as disqualifying would empty the wiki of images. Proposed rule: not disqualifying on its own; if you want it gone it is cheap to batch-blur, because the avatar sits at a fixed position in the app bar. Acting on the recommendation unless you say otherwise. | the recapture backlog, and every future screenshot decision |
| Q-0101 | **ADR-0007 is still `proposed` and the built-but-unregistered list is now eleven** — and the newest four are the ones that *move stock* (`INTERNAL_SALES_GIN_STOCK_OUT`, `INTERNAL_SALES_GRN_STOCK_IN`, `INTERNAL_SALES_INVOICE_NO_STOCK_OUT`, plus delivery-order processing). A customer installing the registered "Sales GIN (Internal)" expecting a warehouse issue document gets a record-keeping form instead. This is no longer a documentation question. | four pages left live pending your call, and the whole ADR |
| Q-0125 | **`internal-consignment-billing-applet` is filed under `applets/sales-workflow/` but documents a purchase-side document. Move it, or leave the URL alone?** Rebuilt from source today: its type is `INTERNAL_PURCHASE_CONSIGNMENT_INVOICE` (short code `CSGINV`, amount signum **−1**), its route is `internal-purchase-invoice`, its entity picker is titled *Select Supplier*, its file import posts to the internal **purchase** invoice endpoint, and its journal credits a creditor and clears `CONSIGNMENT_LIABILITY` against `CONSIGNMENT_STOCK`. Its four sibling consignment pages already live under `purchase-workflow/` and `inventory-workflow/`. Moving it is a structural change (spec + ADR-0002/0006 gate, four inbound links, an alias); leaving it means the sales-workflow index lists an accounts-payable document. I have left it where it is and cross-linked it. | one page move, four inbound links (F-0447) |
| Q-0126 | **Four sales-workflow applets point their registry `documentation_url` at Confluence, not the wiki — repoint them?** `internal_sales_inquiry_applet`, `salesContractApplet` and `recurringSalesInvoiceApplet` link to `bigledger.atlassian.net/wiki/spaces/AKAUN/…`; `SalesReportSupplierAccess` and `internalConsignmentBillingApplet` have **no** `documentation_url` at all. The other twenty-five in the folder point at wiki.bigledger.com. Three of those five now have rebuilt-from-source wiki pages, so the in-product Help link is sending users to a stale Confluence page instead. Who owns `bl_applet_hdr.documentation_url`, and may we submit the five corrections? | the in-product Help link for five applets |
| Q-0136 | **Three certification facts the compliance sweep could not settle.** (a) Is **"LHDN Approved Solution"** a real designation? It was published on `e-invoice-peppol.md` in English and Chinese; nothing in `kb/` records such a list, and MyInvois publishes registered *intermediaries*, not approved solutions. (b) May we state **which cloud provider hosts the platform** and that its data centres are ISO 27001 certified? That is a true statement about the host, not about us, and it is the honest replacement for the "ISO 27001 certified data centers" bullets I removed from `platform-overview.md` and `faqs.md` — but I will not name a provider without your say-so. (c) Does any customer contract **commit to an uptime SLA**? Four pages published 99.9% or 99.99%; if a clause exists, cite it and the pages can say so. | Three sentences on `e-invoice-peppol.md`, `platform-overview.md`, `faqs.md` and `technical-concerns.md`, and the `certification` lint allowlist (F-0462…F-0467) |
| Q-0146 | **Six main-nav entries point at `modules-v2/` sections that do not exist** — `sales-crm`, `erp`, `it-ce`, `service-industry`, `drop-shipping` and `financial-accounting`'s siblings are fine, but those five plus one have no directory under `content/en/modules-v2/`. `hugo.yaml` lists 18 `pageRef: /modules-v2/…` targets; only 12 directories exist. Four of the six missing ones are the invented modules this audit recommends unpublishing, so the fix depends on the Q-0038 answer: drop the nav entries, or build the sections. | six dead items in the site's primary navigation (F-0462) |
| Q-0147 | **`content/en/modules.md` redirects with `<script>window.location.href = "/modules-v2/"</script>`.** CLAUDE.md forbids JavaScript in content files, and a JS redirect is invisible to crawlers, breaks the back button and leaves 386 lines of stale module prose behind it that a reader with JS off will read as current. Should this become a Hugo `aliases:` entry on `modules-v2/_index.md` (the correct mechanism), or does the page have a reason to exist? | the canonical `/modules/` landing URL, and whichever tree wins Q-0038 (F-0463) |
| Q-0148 | **`modules/budgetary.md` embeds 35 screenshots as base64 `data:` URIs, making one markdown file 1.0 MB.** I viewed all 35: they are test-tenant captures (`TESTING` sidebar, items named `Marketing`/`sales`/`TEST`) with no personal names, brands or real amounts — **not a privacy problem**. But they bypass the CDN pipeline entirely, so no image check can see them, they cannot be quarantined by the normal mechanism, and they cannot be recaptured without re-encoding. Extract to `static/images/` and re-link, or does the page go under Q-0038 anyway (it is a 1,052-line applet manual duplicating the sourced `applets/finance/budgetary-applet.md`)? | one 1 MB published page; the image-review invariant (F-0464) |
