# Lane 1 findings

Folders: sales-workflow, membership, manufacturing, claims

## Cross-lane link requests


- From `sales-workflow/sales-report-applet` (done 2026-09-05, run 2): add `sales-report-applet` to `related_applets` of `finance/internal-receipt-voucher-applet` and `finance/internal-payment-voucher-applet` (collection reports read their settlement lines), `master-data/inv-item-maintenance-applet` (replacement / manual / reference / delta / rebate prices and report unit costs are the "Calculate Base On" bases; categories 1–20), `master-data/pricebook-applet`, `master-data/customer-applet` (customer category / state filters), `master-data/cashbook-applet`, `master-data/organisation-applet`, `inventory-workflow/non-stock-and-trade-in-applet`, `sales-workflow/internal-sales-credit-note-applet` and `internal-sales-debit-note-applet` (lane 1 — will add on next touch).

- From `membership/membership-admin-applet` (done 2026-09-05, run 2): add `membership-admin-applet` to `related_applets` of `master-data/organisation-applet` — and document there the **company member-point configuration** (`member_point_award_doc_in` document type + `member_point_in_doc_status` posting status) because the backend awards points only when a document matches it; `master-data/customer-applet` (member ↔ customer link); `master-data/doc-item-maintenance-applet` (point adjustments need an item; POS needs a `MEMBER_POINT_DISCOUNT` item).


- From `sales-workflow/pos-general-applet` (done 2026-09-05, run 2): add `pos-general-applet` to `related_applets` of `master-data/organisation-applet` (and state that the branch **default location** is enforced at cash-bill FINAL — `GENERIC_DOC_BRANCH_DEFAULT_LOCATION_DOES_NOT_MATCH`), `master-data/customer-applet` (credit-limit / credit-term blacklist blocks FINAL of cash bill, invoice, order and outbound DO — `ENTITY_BLACKLISTED`; spending-limit lines apply to cash bills), `master-data/cashbook-applet`, `master-data/chart-of-account-applet`, `master-data/tax-configuration-applet`, `master-data/pricebook-applet` (free-gift rules are a pricebook of type `POS_FREE_GIFT`; role ↔ pricing-scheme link), `master-data/inv-item-maintenance-applet`, `master-data/doc-item-maintenance-applet` (adjustment items of txn type `DOC_HEADER_ADJUSTMENT`, `GROUP_DISCOUNT`, `MEMBER_POINT_DISCOUNT` are required by POS), `inventory-workflow/stock-balance-applet`, `inventory-workflow/non-stock-and-trade-in-applet` (trade-in lines from POS carry `INTERNAL_PURCHASE_TRADE_IN`), `finance/bank-reconciliation-applet` (updating / finalising a bill whose settlement line is reconciled returns an unreconcile error), `finance/internal-receipt-voucher-applet`, `e-invoice/my-e-invoice-portal-applet` (cash bill = e-Invoice type 01; consolidated by branch), `membership/membership-admin-applet`, `membership/membership-points-currency`, `membership/voucher-management-applet` (all lane 1 — will be done when reached).

- From `sales-workflow/internal-sales-debit-note-applet` (done 2026-09-05): add `internal-sales-debit-note-applet` to `related_applets` of `purchase-workflow/internal-purchase-debit-note-applet` (intercompany mirror), `finance/internal-receipt-voucher-applet`, `e-invoice/my-e-invoice-portal-applet`, `master-data/doc-item-maintenance-applet` (account-code items for charges), `master-data/customer-applet`, `master-data/chart-of-account-applet`, `master-data/cashbook-applet`, `master-data/tax-configuration-applet`.

- From `sales-workflow/internal-sales-return-applet` (done 2026-09-05): add `internal-sales-return-applet` to `related_applets` of `purchase-workflow/internal-purchase-return-applet` (intercompany mirror), `e-invoice/my-e-invoice-admin-applet` and `e-invoice/my-e-invoice-portal-applet` (return references the original e-invoice; void blocked after submission), `finance/internal-receipt-voucher-applet`, `inventory-workflow/stock-balance-applet`, `master-data/customer-applet`, `master-data/chart-of-account-applet` (SALES_RETURN default GL), `master-data/cashbook-applet`.

- From `sales-workflow/internal-sales-order-applet` (done 2026-09-05): add `internal-sales-order-applet` to `related_applets` of `purchase-workflow/internal-purchase-order-applet` (intercompany mirror + auto stock purchase), `inventory-workflow/stock-availability-applet` and `inventory-workflow/stock-reservation-applet` (open orders reduce availability; lines reserve), `delivery-installation/delivery-installation-applet` (delivery plans/calendar), `finance/internal-receipt-voucher-applet` (deposits), `master-data/customer-applet`, `master-data/pricebook-applet`, `master-data/cashbook-applet`, `master-data/workflow-design-applet`.
- `inventory-workflow/stock-availability-applet`: state that available quantity subtracts open FINAL sales orders (signum −1 in `StockAvailabilityService`) as well as invoices.

- From `sales-workflow/internal-sales-credit-note-applet` (done 2026-09-05): add `internal-sales-credit-note-applet` to `related_applets` of `finance/internal-receipt-voucher-applet`, `purchase-workflow/internal-purchase-credit-note-applet` (intercompany mirror), `e-invoice/my-e-invoice-portal-applet` (original-invoice reference fields), `master-data/customer-applet`, `master-data/chart-of-account-applet`, `master-data/cashbook-applet`, `master-data/tax-configuration-applet`, `master-data/workflow-design-applet`.

- From `sales-workflow/internal-sales-invoice-applet` (done 2026-09-05): please add `internal-sales-invoice-applet` to `related_applets` of these pages outside lane 1: `finance/internal-receipt-voucher-applet`, `finance/internal-purchase-invoice-applet` (intercompany mirror), `e-invoice/my-e-invoice-portal-applet`, `master-data/customer-applet`, `master-data/chart-of-account-applet` (company default GL codes DEBTOR/SALES/OUTPUT_TAX/COGS), `master-data/cashbook-applet` (settlement methods need cashbook + GL), `master-data/pricebook-applet`, `master-data/tax-configuration-applet`, `master-data/workflow-design-applet`, `master-data/organisation-applet`, `inventory-workflow/stock-balance-applet`.
- `master-data/chart-of-account-applet`: its Configuration should state that journal posting of sales documents throws `MISSING_DEFAULT_GL_CODE: DEBTOR|SALES|SALES_DISCOUNT|OUTPUT_TAX|COGS|FOREX_GAIN|FOREX_LOSS` when the company default GL link is missing (source: JournalPostingTypeHandler.java SALES handler; JournalPostingService.java).
- `master-data/cashbook-applet`: settlement methods (`STL_MTHD` lines) must carry both a cashbook and a GL code — posting fails with `MISSING_CASHBOOK` / `MISSING_GL_CODE: STL_MTHD [code]` (JournalPostingService.java L191-L211).

## Registry / naming mismatches


- `salesReport` (Sales Report): 24 `SHOW_ADVANCED_SEARCH_*` client-side defs are registered, but the shared advanced search no longer reads them (code comment: gate removed because the defs "are never seeded platform-wide"); `ALLOW_VIEW_TRANSACTION_ALL_USER_CREATE` registered, not read; no `SHOW_VOUCHER_REDEMPTION_MENU`, `SHOW_NET_MARGIN` or `SHOW_DELTA_*` defs although the app computes those keys. `SHOW_SALES_REPORT_BY_DAILY_WEEKLY_MONTHLY_MENU` is registered twice.
- `MEMBERSHIPADMIN` (Membership Admin): registry doc URL is the alias `/applets/membership-admin-applet/` (works via `aliases:`); only 2 client-side defs. Setting `SHOW_DOCUMENT_DELETE_BUTTON` appears four times on the Field Settings screen but no component reads it.


- `posGeneral` (POS General): page title was "POS General Applet"; set to registry name "POS General". App reads client-side codes `POS_SHOW_Z_REPORT`, `POS_SHOW_CASHIER_COLECTION_REPORT`, `EXCLUDE_ACCOUNT_CODE_ITEM_TYPE_AT_ITEM_SEARCH` and the `ALLOW_<TYPE>_ITEM_PRICE_EDIT` family — none among its 31 ACTIVE defs (while `POS_SHOW_AUDIT_TRAIL_REPORT` *is* registered). Conversely `POS_CLOSE_SESSION`, `POS_FLOAT_CASH_IN_OUT`, `POS_ATTACH_DRAWER` are registered but never read by the UI code. Two DELETED defs (`ALLOW_SELL_BELOW_UNIT_PRICE_STD_INCL_TAX`, `POS_REMOVE_ROUNDING`).

- `InternalSalesDebitNote`: two registry rows share this code — ACTIVE "Sales Debit Note (Internal)" and DELETED "Internal Sales Debit Note". Page uses the ACTIVE name. The parity check should key on (code, status).
- `InternalSalesDebitNote`: `SHOW_FILE_IMPORT_MENU`, `SHOW_INTERCOMPANY_MENU`, `SHOW_DRAFT_BUTTON` and the clone-button permission are read by the app but absent from its 35 client-side perm defs.

- `internalSalesReturnApplet`: registry `name` is "Sales Return (Internal) " with a trailing space (also `salesContractApplet` → "Sales Contract Applet "). Page titles use the trimmed name; the parity check should compare trimmed strings.
- `internalSalesReturnApplet`: `SHOW_INTERCOMPANY_MENU` / `SHOW_FILE_EXPORT_MENU` read by the app, not among its 45 client-side perm defs (same systemic gap).

- `erp_internal_sales_order_applet_v2` (Sales Order (Internal)): app.component reads `SHOW_SO_DETAILED_REPORT_MENU`, `SHOW_PICK_PACK_QUEUE_MENU`, `SHOW_DELIVERY_ITEMS_MENU`, `SHOW_ECOMSYNC_MENU`, `SHOW_BATCH_PRINTING_MENU`, `SHOW_DELIVERY_PLAN_MENU`, `SHOW_CALENDER_MENU`, `SHOW_FILE_EXPORT_MENU`, and line approval reads `ALLOW_APPROVE_SELL_BELOW_PRICE`; none of these exist among the 25 client-side perm defs for the applet. Same pattern as the credit note — looks systemic (per-tenant seeding?).

- `InternalSalesCreditNote` (Sales Credit Note (Internal)): the app reads client-side permission codes `SHOW_FILE_IMPORT_MENU`, `SHOW_INTERCOMPANY_MENU`, `SHOW_GENDOC_CLONE_BUTTON` and the `ALLOW_<TYPE>_ITEM_PRICE_EDIT` family, but none of them exist in `bl_applet_client_side_perm_dfn` for this applet (38 ACTIVE defs). Either the registry is incomplete or these are seeded per tenant. Documented in the page's Troubleshooting; needs a product decision.

## Undocumented applets encountered


- `posCustomerDisplay` — "POS Customer Display" (TNT-USER, ACTIVE, doc URL on Confluence). Repo `blg-applet-wavelet-pos-customer-display-applet` exists. No wiki page (blg-wiki#126 asked for one); POS General's `CUSTOMER_DISPLAY` / `DISPLAY_POLE_URL` settings depend on it. Not in the lane-1 queue — needs a folder decision (sales-workflow?).

- `SFA` — "Sales Force Automation" (TNT-USER, ACTIVE, doc URL points at Confluence). No wiki page under sales-workflow. Not in lane-1 queue.
- `shopee_sales_order_applet` — registry name "Tiktok Sales Order Applet" (TNT-ADMIN, ACTIVE, 2024-11-20). No wiki page; the code/name mismatch itself needs a decision (marketplace integration applets may fall under the customer-specific/integration exclusion policy).

## Questions for Vincent


7. **Personal data in a source repo.** `blg-applet-wavelet-membership-admin-applet/…/src/assets/templates/Membership_Template_Format.csv` ships a sample row that looks like a real person (name, IC number, phone, email). I used only the header row. Someone should replace the sample row in the repo.
8. **Sales Report page had invented features.** The previous page described "calculation sets", a "My Sales" menu, favourites and Excel/BI integration settings that do not exist. Replaced with the real applet-own settings screen. Same for Membership Admin (physical/virtual card types, date-format setting, custom-status earn/redeem flags, currency symbol/decimals/transferability). Flagging in case those texts came from a product brief rather than imagination.


5. **POS page video.** The old page embedded YouTube with an inline-styled `<div>/<iframe>` (against the Hextra rule). I replaced it with Hugo's `{{< youtube Gf6gXUfHebE >}}` shortcode (already used on three finance pages). `daily-cashier-report-applet` has the same raw iframe — I will convert it when I reach that page unless you prefer the raw embed.
6. **Tenant-specific branches in POS code.** The offline-settings component has `*ngIf="tenantCode==='<tenant>'"` branches for one customer's offline-print / sync variant. I documented only the generic path. Fine?

1. **Client-side permission registry gap (systemic).** All five applets read `SHOW_*_MENU` (and in places `SHOW_DRAFT_BUTTON`, `SHOW_GENDOC_CLONE_BUTTON`, `ALLOW_APPROVE_SELL_BELOW_PRICE`, `ALLOW_<TYPE>_ITEM_PRICE_EDIT`) from client-side permissions, but `bl_applet_client_side_perm_dfn` in akaun_master does not define them for those applet codes. Either the master registry is incomplete (documented as "request the code" in each page's Troubleshooting) or they are seeded per tenant. Which is it, and should the pages say so differently?
2. **Registry hygiene.** Trailing spaces in `name` for `internalSalesReturnApplet` and `salesContractApplet`; a DELETED duplicate row for code `InternalSalesDebitNote`. Should the parity check trim names and key on (code, status)?
3. **Sales Order page banner.** I removed the "Under Review: This applet is still under review" callout because the page is now source-derived. Confirm that is what the banner meant.
4. **Marketplace image filenames.** `so-shopee-ecomsync.png` / `so-lazada-ecomsync.png` (pre-existing) name third-party marketplaces in the filename; captions now say "marketplace A/B". Fine to keep, or rename the files?

## Notes

- 2026-09-05 — **Learning 1: the applet's own settings surface is tiny; the real configuration lives in shared-utilities.** Every document applet exposes a small *Default Selection* screen (branch, location, sometimes pricing scheme / pricebook / currency / decimal precision / `DATE_TXN_LOGIC`, plus drag-and-drop tab order) and delegates *Application Settings* to `blg-shared-utilities/modules/permission/field-configuration` — an 8,300-line template gated by `sessionStorage.appletCode`. The set of toggles that actually apply to an applet is the intersection of that template with the applet's `applet-settings.model.ts` (Sales Invoice 255, Sales Order 240, Debit Note 163, Credit Note 194, Sales Return 134). The template also shows toggles an applet ignores (e.g. `HIDE_PICK_PACK_QUEUE_MENU` on the credit note, purchase-order menus on the debit note). The lane scratchpad has `applet-scan.sh` that does this intersection automatically.
- 2026-09-05 — **Learning 2: `HIDE_*` settings and `SHOW_*` client-side permissions are a pair.** app.component removes a sidebar item when `!SHOW_X && HIDE_X`, so a tenant-wide hide can be re-opened per user/role — but only if the `SHOW_*` code exists in the registry, which it mostly does not (see Questions). The same pairing exists for pricing columns (`HIDE_UNIT_PRICE_*` ↔ `SHOW_UNIT_PRICE_*`), and for this family of applets the shared screen defaults detailed pricing columns and department fields to hidden (`shouldHideSetting` / `hideDepartmentSetting` lists).
- 2026-09-05 — **Learning 3: posting behaviour is not an applet setting.** What FINAL does is decided by the document's signums (invoice +1/−1, credit note −1/0, return −1/+1, debit note +1/0, order 0/0), by `JournalPostingTypeHandler`'s per-family handler (`SALES` → DEBTOR/SALES/SALES_DISCOUNT/SALES_RETURN/OUTPUT_TAX/FOREX_*; return lines use the `SALES_RETURN` txn code), and by the company's `posting_final_json` include/exclude list read by the Generic Document Primary Processor. The only applet-side levers are validation flags (`VALIDATE_STOCK_ON_FINALIZE`, `ENABLE_SERIAL_NUMBER_VALIDATION_FINAL`, `DISALLOW_SELL_BELOW_*`) and `FINAL_STATUS_GUID` for workflow.
- 2026-09-05 — Applet repos hold almost no GitHub issues (1–6 each, mostly migration chores); real failure modes come from commit subjects that reference customer support repos. Troubleshooting tables were built from those, anonymised.
- 2026-09-05 — Two pages in this lane carried a UTF-8 BOM (sales return, debit note); preserved.
- 2026-09-05 — Stopping after 5 pages this run (invoice, credit note, order, return, debit note): the next queue item, `pos-general-applet`, is a much larger applet (its own 80+ `POS_*` toggles on the shared screen) and deserves a fresh session.

- 2026-09-05 — Applet UI repos are Angular workspaces under `micro-fe/projects/wavelet-erp/applets/<slug>/`; the Settings → *Application Settings* screen is the **shared** `FieldConfigurationComponent` from `blg-shared-utilities` (8,300-line template, gated by `sessionStorage.appletCode`). Per-applet settings therefore = intersection of the applet's `AppletSettings` interface with that template. Reusable extraction script kept in the lane scratchpad.
- 2026-09-05 — The applet repo has only 2 GitHub issues; real failure modes come from the commit log, whose subjects reference customer support repos (`blg-sd-<customer>`). All anonymised.

- 2026-09-05 (run 2) — **POS General is the exception to the "applet-settings.model.ts is the key set" rule.** Its model declares 38 keys but the code reads ~200 via `master?.X`; `applet-scan.sh` therefore reports only 11 shared toggles. The true intersection (grep every `master|settings|personal|resolve.X` read in the applet ∩ shared-template controls) is **155**, ~70 of them in the posGeneral-only "POS Settings" panel (`field-configuration.component.html` L4932–5302) plus an "Auto UI" panel and a "Cashier Collection Report" tab. Consider adding that second strategy to `applet-scan.sh` as a fallback when the model is thin.
- 2026-09-05 (run 2) — POS behaviour that is **backend-fixed**, not a setting: sum(PNS) must equal sum(STL_MTHD)+contra at FINAL; location must equal the branch default location; blacklist check; zero-amount bill gets an auto zero settlement line; reward/redeem points generated on save for member bills (using `posGeneral` settings even when the document is an invoice from `salesInvoiceApplet`).
- 2026-09-05 (run 2) — POS menu items are gated by **setting AND permission pairs** (Approval, Settlement Adjustment, Swap Serial) or **setting unless permission** (Z Report, Cashier Collection, Audit Trail) — a cleaner version of the HIDE_/SHOW_ pattern noted in run 1.
- 2026-09-05 (run 2) — **Report and master-data applets do not use the shared Field Configuration screen.** Sales Report and Membership Admin each ship their *own* `field-configuration` component under `settings-container/`; `applet-scan.sh` reports 0–1 shared toggles for them, which is correct — read the local component instead. Sales Report generates its menu-hide toggles from `menuItems` (`HIDE_<ROUTE>_MENU`) and gates "Calculate Base On" options on `ADVANCED_SEARCH_SHOW_<OPTION>` settings.
- 2026-09-05 (run 2) — **Membership points are awarded by a company-level config, not by the Membership Admin applet**: `MemberPointDocInConfig` (doc type + posting status) on the company; documents without `point_amount`/`point_currency` on lines stay unposted for retry. Expiry is a scheduled processor over control-account rows; void adds a shortfall adjustment.
- 2026-09-05 (run 2) — Stopping after 3 pages this run (POS General, Sales Report, Membership Admin). Next: `internal-sales-quotation-applet` (document applet — the run-1 method applies directly).

## Run 4 (2026-09-05) — rework verification of `sales-workflow/internal-sales-invoice-applet`

- Verified run 3's rework against the code: the documented Application Settings set equals (rendered on the shared screen for `salesInvoiceApplet`) ∩ (read outside the model) — zero diffs by script; `VALIDATE_STOCK_ON_FINALIZE` is described as the client confirm dialog (`stock-validation.service.ts` L47–L75) and `SHOW_ITEM_STOCK_BALANCE` → `validate_stock_balance` (default true when absent, `GenericDocumentService.java` L390–L392) as the backend switch; posting proof block present with DCO/handler/service line cites; `ALLOW_NEGATIVE_STOCK_WITH_CONFIRMATION` demoted to model-only.
- **Correction made in run 4 (direction of an earlier claim):** the page said an unmapped `SALES` / `OUTPUT_TAX` / `COGS` default "does not throw: the journal line is written without a GL code". The service indeed does not throw (`orElse(null)` / `ifPresent`), but the platform liquibase changeset `202104141527` adds `CHECK (glcode_guid IS NOT NULL) NOT VALID` on `bl_fi_jrnl_line`, so the insert fails and the posting job errors with a database message. Fixed in the prerequisites row, the COGS proof row and the Troubleshooting row; `liquibase-changelog.sql` added to `sources.lifecycle` / `sources.troubleshooting`.

### Cross-lane link requests (run 4)
- `master-data/chart-of-account-applet` (and any purchase-side page that copied the wording): `MISSING_DEFAULT_GL_CODE: <code>` is thrown only for the ARAP entity line (DEBTOR / DEBTOR_NON_TRADE / CREDITOR / CREDITOR_NON_TRADE — five throw sites in `JournalPostingService.java` L339, L412, L448, L490, L526). Missing `SALES`, `OUTPUT_TAX`, `INPUT_TAX`, `COGS`, `PURCHASE` defaults fail later on the `bl_fi_jrnl_line.glcode_guid_not_null_check` constraint. Please do not list them as `MISSING_DEFAULT_GL_CODE` messages.
- Sales guides (`/guides/sales-guides/*`): if any says "enable *Validate stock on finalize* to block negative stock", correct it — that key only adds a confirmation dialog; the backend hard stop is on unless `SHOW_ITEM_STOCK_BALANCE` was saved off.

## Run 4 (2026-09-05) — `sales-workflow/internal-sales-quotation-applet` (done)

- The old page was a 1,434-line invented user guide: "Pending Approvals", "Team Reports", "Quote Templates", "Pricing Schemes" settings pages, an approval-delegation period, a "Convert to Order" button and a named persona ("Gracia") — none exist in the applet or backend. Rewritten to the standard; four real screenshots kept (listing, create Main tab, Application Settings landing, File Export), the rest dropped (see Questions).
- Zero-signum document: no journal, no stock, no availability effect, no blacklist check, no e-Invoice. The **Convert** tab is *Convert to Internal Receipt Voucher* — it creates an RV from the quotation's Collection lines and then **deletes the quotation** (verified in `internal-sales-quotation.effects.ts` L993–L1050 and the DELETE controller). Not a conversion to a sales order.
- Method note: the model-based `applet-scan.sh` reported 94 shared toggles; a gating-aware pass (walking each control's `*ngIf="appletCode…"` ancestors) plus dynamic-key reads (`hide:'KEY'`, listing `setting:'KEY'`) gives 100 documentable keys, 15 rendered-but-unread and 11 read-but-rendered-only-for-other-codes (e.g. `HIDE_CREDIT_TERMS`, `HIDE_QUOTATION…HIDE_OTHERS`). Worth folding the ancestor check into `applet-scan.sh`: without it, gated controls (`HIDE_CREDIT_TERMS` for the SO only, reference-column toggles for purchase applets) get documented as if they existed for this applet.
- Shared-screen template bug worth a product ticket: `ENABLE_EDITING_UNIT_PRICE_STD` is guarded by `appletCode==='posGeneral' || 'salesInvoiceApplet'` (`field-configuration.component.html` L3053, L6887) — the string literal is always truthy, so the control renders for every applet.

### Cross-lane link requests (run 4, quotation)
- `finance/internal-receipt-voucher-applet`: add `internal-sales-quotation-applet` to `related_applets` and state that the quotation's *Convert to Internal Receipt Voucher* action creates an RV (amount signum −1) from the quotation's `STL_MTHD` lines, copying the quotation header **including its posting status**, then deletes the quotation.
- `inventory-workflow/stock-availability-applet`: state that `INTERNAL_SALES_QUOTATION` is not in `StockAvailabilityService`'s reducing list — quotations never reduce availability; only FINAL sales orders and invoices do.
- `purchase-workflow/internal-purchase-quotation-applet`: add `internal-sales-quotation-applet` as the selling-side twin (same zero-signum behaviour).
- `master-data/customer-applet`, `master-data/organisation-applet`, `master-data/pricebook-applet`, `master-data/tax-configuration-applet`, `master-data/cashbook-applet` (Branch Settings default settlement method per payment type), `master-data/workflow-design-applet`, `inventory-workflow/inv-item-maintenance-applet`, `inventory-workflow/stock-balance-applet`: add `internal-sales-quotation-applet` to `related_applets`.
- `modules-v2/pos/related-applets/_index.md` (and the POS module copy generally, other lane): the quotation card says "customer deal negotiation" — fine — but any "one-click conversion to order" wording elsewhere should read "knocked off by the Sales Order".
- Lane 1 (self, on next touch): `internal-sales-inquiry-applet`, `internal-jobsheet-applet`, `car-workshop-applet` should list the quotation.

### Registry / naming mismatches (run 4)
- `InternalSalesQuotationApplet`: page title was "Sales Quotation (Internal) Applet"; set to the registry name. Only **2** client-side perm defs seeded (`HIDE_PRICE`, `SHOW_TRANSACTION_DATE`) although the code checks ~50 (`SHOW_GENDOC_FINAL/VOID/DISCARD_BUTTON`, `SHOW_DRAFT_BUTTON`, all listing `SHOW_*` column codes, pricing `SHOW_*`, `EXCLUDE_ACCOUNT_CODE_ITEM_TYPE_AT_ITEM_SEARCH`). Same systemic gap as Question 1.

### Questions for Vincent (run 4)
9. **Screenshots with test-tenant data.** `static/images/internal-sales-quotation-applet/edit-form.png` shows a real-looking business entity name and e-mail on the Account tab, and `quote-templates.png` lists printable-format codes that are people's first names. I did not embed either. Should they be re-captured with synthetic data, or deleted? (`team-reports-page.png` is a blank page and `create-lines.png` duplicates `create-form.png` — both safe to delete; `sales-quotation-overview-infographic.png` illustrates the invented "1-click orders" story and is no longer referenced.)
10. **Convert-to-receipt-voucher semantics.** Convert copies the quotation's `posting_status` into the new RV header and then deletes the quotation, with no DRAFT-only guard on the tab. If a FINAL quotation is converted, the RV is created with a FINAL header. Product question: intended, or should the UI restrict Convert to DRAFT quotations? Documented as-is.
11. **Template bug in shared utilities.** `ENABLE_EDITING_UNIT_PRICE_STD` guard is always true (see method note). Worth an issue on `blg-shared-utilities`.

## Run 4 (2026-09-05) — `sales-workflow/car-workshop-applet` (done)

- The old page was an invented user guide (manager and finance "playbooks", approval flows, "tenant-dependent" settings). Rewritten to the standard from the applet, the ASV backend (`bl_asv_*` tables, `AsvConsultation*` services/DCOs, `TntAsvPermissions`) and the schema DDL.
- **Key finding: Settings → Field Settings is a non-functional stub.** `settings/field-settings` routes to the applet's own 50-line `FieldConfigurationComponent` (unbound toggles, empty class, SAVE wired to nothing), not to the shared screen. Only Default Selection (`DEFAULT_BRANCH`, `DEFAULT_LOCATION`, `DEFAULT_COMPANY`, `CONSULTATION_DETAILS_TAB_ORDER`) and Checklist Settings persist. The embedded jobsheet/quotation/SI/PI/RV forms read 146 setting keys under `appletCode = carWorkshopApplet` that no screen can write — documented as "read at runtime without a control", pointing at the standalone pages.
- The consultation is not a generic document (no signums, no posting); its "Posting Status" (OPEN/CLOSE/DRAFT) is a free label. Documents created from its tabs get a `bl_asv_consultation_gendoc_hdr_link`; changing the vehicle rewrites the plate on every linked document.
- **No screenshots embedded.** All 15 files under `static/images/car-workshop-applet/` show person names (consultants, members, technician "KTEST" aside), an e-mail address, a phone number, a tenant-prefixed branch code, or vehicle brand names in checklist names. Left in place, unreferenced.

### Cross-lane link requests (run 4, car workshop)
- `finance/internal-receipt-voucher-applet`, `finance/internal-purchase-invoice-applet`: add `car-workshop-applet` to `related_applets` ("created from a workshop consultation's Receipt / Purchases tab and linked by `bl_asv_consultation_gendoc_hdr_link`").
- `master-data/customer-applet`: mention that vehicles (`bl_fi_mst_entity_vehicle_hdr`, API `entity-vehicle-hdrs`) hang off a customer entity and are created from the workshop's Select Vehicle panel; add `car-workshop-applet`.
- `master-data/organisation-applet`, `membership/membership-admin-applet` (lane 1 self): add `car-workshop-applet`.
- Lane 1 (self, queued): `internal-jobsheet-applet` should say Car Workshop embeds the same document type; `internal-sales-quotation-applet` already lists it.

### Registry / naming mismatches (run 4, car workshop)
- `carWorkshopApplet`: `documentation_url` points at a Confluence page although the wiki page exists at `/applets/sales-workflow/car-workshop-applet/`. Zero client-side perm defs seeded while the embedded document forms check the usual `SHOW_*` / `ALLOW_*` codes. The route root in code is `car-worshop-applet` (typo) — documented as-is.

### Questions for Vincent (run 4, car workshop)
12. **Field Settings stub.** Is the placeholder screen intentional? If the embedded documents are meant to follow the standalone applets' settings, the code would have to read those applet codes instead of `carWorkshopApplet`. Either way the page now says plainly that the screen saves nothing — confirm you are happy publishing that.
13. **Screenshots.** Re-capture the 15 workshop screenshots on a tenant with synthetic names (no real consultants/members/e-mails, generic checklist names), or accept the page without images for now.

- 2026-09-05 (run 4) — Stopping after the invoice rework verification plus two large pages (Sales Quotation, Car Workshop). Next in queue: `claims/claim-applet.md` then `claims/claim-cycle-applet.md` — a different applet family (claims / OCR), which deserves a fresh session. Method note for the next run: run the gating-aware census (ancestor `*ngIf="appletCode…"` walk + dynamic-key reads) before trusting `applet-scan.sh`'s model-based count; and check whether `settings/field-settings` routes to the shared component or a local stub before documenting any shared toggle.
